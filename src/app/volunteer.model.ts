export class Volunteer {
  constructor(public name: string, public quote: string, public country: string, public region: string, public content: string, public category: string, public image: string) { }
}
