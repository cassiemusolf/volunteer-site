export class Volunteer {
  constructor(public name: string, public quote: string, public country: string, public region: string, public story: string, public category: string, public image: string) { }
}
